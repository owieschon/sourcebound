participant_id: 970fe2c3-a376-4b56-a68c-474d3c9bfe45
profile: codex-gpt-5-5-high
independent: true
context: published-docs-only
completed_at: 2026-07-14T01:36:16Z
task: explain-limitation
result: PASS

Evidence:
Answer before grading:
  sourcebound itself does not revoke host network access for an allowlisted process. Its process controls are not an operating-system sandbox. Network isolation belongs to the surrounding execution environment, so an allowlisted executable can open a network connection if the runner permits it.

Citations:
  README.md, section "Current limits": states that declared commands and plugins run in disposable repository copies with minimal environments, active I/O limits, and timeouts, and that network isolation belongs to the execution environment.

  docs/SECURITY_MODEL.md, section "Host boundary": states that the process controls are not an operating-system sandbox and that a declared executable can open a network connection or address an absolute host path if the surrounding runner permits it. The same section says to run untrusted declared code in a network-blocked container or equivalent OS sandbox.

Documentation defects encountered:
  none
