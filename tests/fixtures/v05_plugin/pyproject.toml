[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "sourcebound-fixture-plugin"
version = "1.0.0"
description = "External fixture package for the sourcebound plugin protocol"
requires-python = ">=3.10"
